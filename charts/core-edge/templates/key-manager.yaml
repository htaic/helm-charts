{{- if .Values.keymanager }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keymanager
spec:
  replicas: {{ .Values.edgeccs.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include ".helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include ".helm.selectorLabels" . | nindent 8 }}
        app: keymanager
    spec:
      {{- if .Values.keymanager.volume }}
      volumes:
        - name: {{ .Values.keymanager.volume.pvcName }}
          persistentVolumeClaim:
            claimName: {{ .Values.keymanager.volume.pvcName }}
      {{- end }}
      containers:
        - name: keymanager
          {{- if .Values.keymanager.image.digest }}
          image: {{ .Values.keymanager.image.repository }}@{{ .Values.keymanager.image.digest }}
          {{- else }}
          image: {{ .Values.keymanager.image.repository }}:latest
          {{- end }}
          imagePullPolicy: Always
          volumeMounts:
          - name: cert
            mountPath: /cert
      volumes:
        - name: cert
          configMap:
            name: "{{ include ".helm.fullname" . }}-certs"
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets }}
      {{- end }}
{{- end }}
